{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug (CodeLLDB)",
      "cargo": {
        "args": ["build", "--bin=hydrogen", "--package=hydrogen"],
        "filter": {
          "name": "hydrogen",
          "kind": "bin"
        }
      },
      "envFile": "${workspaceFolder}/dev/.env",
      "cwd": "${workspaceFolder}"
    }
  ]
}
